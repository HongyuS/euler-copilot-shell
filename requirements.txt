# Dependencies are managed with uv. This file is kept for compatibility with
# tooling that still expects pip-style requirement lists.
#
# To regenerate this file from the locked dependency set, run:
#   uv export --format requirements-txt --no-dev --frozen > requirements.txt
